#include <stdio.h>

int main() {
    printf("Hello World\n");
    return 0;
}

/*
steps to upload codes on github
1. git status (check git is initialized or not)
2. git init (to initialize a new git repository)
3. git add . (to stage all changes)
4. git commit -m "your commit message" (to commit changes)
5. git push (to push changes to remote repository)
*/